# The level generator

> **Placeholder.** This page is owed and not yet written. It will document the rules engine that builds every shore and every course from a seed — the R-rules stated verbatim from `rules.ts`, the search in `generate.ts`, what `compile.ts` bakes, and the analyzer's bands that reject a level, and it syncs from `engine/mapgen/rules.ts` (verbatim), `generate.ts`, `compile.ts` and `engine/analysis/`, judged with `make level` and `make analyze` — the `update-docs` skill maps that source to this page, and a change there is a change here in the same PR.
