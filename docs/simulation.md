# Simulation and the bot

> **Placeholder.** This page is owed and not yet written. It will document the headless harness — how the bot reads the course and rides it, what `simulateStage` reports, what every column of the `make sim` table means and which movements are regressions, and the determinism digest, and it syncs from `engine/sim/bot.ts`, `engine/sim/simulate.ts` and `scripts/simulate-run.mjs` — the `update-docs` skill maps that source to this page, and a change there is a change here in the same PR.
