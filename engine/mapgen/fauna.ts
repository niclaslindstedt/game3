// SPDX-License-Identifier: PolyForm-Noncommercial-1.0.0
// THE FAUNA — placeholder. What will live here: the fish and the animals
// in the sea and on the rocks, placed per level from the biome's rows the
// way the rocks are (R17's pattern): schools in the deep water off the
// reefs, seals hauled out on the skerries, the birds over the headlands —
// each a seeded, deterministic placement the craft and the renderer both
// read from the level, never regenerated. Nothing is built this session;
// `engine/game/defs/fauna.ts` will carry the catalog and this file the
// placer. The biome table (`biomes.ts`) is where a coast will say what
// swims in it.

export {};
