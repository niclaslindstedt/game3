// SPDX-License-Identifier: PolyForm-Noncommercial-1.0.0
// THE WEATHER — placeholder. What will live here: the sky a level is
// ridden under, drawn per seed from the biome's own chart — clear, high
// cloud, overcast, rain and the squall — with its light, its visibility
// and the RAIN's effect on the water read by the sea and the renderer
// alike. Until then a level carries only its hour (R13) and its wind
// (R12), and the sky system in `pwa/` draws a placeholder light from the
// hour. The rule for it will take an R-number in `rules.ts` when it is
// written, and the doc's mirror will carry it the same day.

export {};
