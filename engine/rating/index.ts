// SPDX-License-Identifier: PolyForm-Noncommercial-1.0.0
// LEVEL RATING — placeholder. What will live here: whether a generated
// level is any GOOD as a race, which is a different question from whether
// it is broken (that is `engine/analysis/`). Trait bands with a floor as
// well as a ceiling — how much of the course is in a bay's calm and how
// much in the open swell, how the air gates are spaced through it, how
// tight the line has to be between the rocks, the fetch the wind gives the
// last kilometre — and the character fingerprint a campaign ladder is built
// out of. Nothing is built this session; `analyzeLevel` is the only judge.

export {};
