// SPDX-License-Identifier: PolyForm-Noncommercial-1.0.0
// PLACEHOLDER — the AUDIO will live here: the engine's note and the pump's
// whine as beds off `rpm` and `throttleEff`, the hull slapping the chop off
// `wetted` and the landings, the wind in the rider's ears, a gate's chime
// and the ring's whoosh, and the scores. Everything synthesized from
// authored parameters, nothing a file, one module touching WebAudio — the
// `sound-effects` and `soundtrack` crafts. Future work: this slice is silent.
export {};
