// SPDX-License-Identifier: PolyForm-Noncommercial-1.0.0
// PLACEHOLDER — the PLAYER'S SETTINGS will live here: the key bindings the
// input manager reads (input.ts carries a fixed table until then), the
// touch layout, the camera the run opens on, the video budget and the
// volume — one table with defaults, read and written through storage
// versioned as OSS_GAME_SPEC §36 asks. Future work.
export {};
