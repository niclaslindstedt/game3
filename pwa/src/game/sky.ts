// SPDX-License-Identifier: PolyForm-Noncommercial-1.0.0
// PLACEHOLDER — the SKY will live here: the sun's position from the level's
// `hour` and the colour of the light it makes, the sky dome and its
// gradient, the clouds, the haze over the water, the weather the level was
// generated with, and the height fog every material carries. That is the
// `atmosphere` craft, and it is future work.
//
// Until it lands, `renderer.ts` sets up the stand-in: a flat sky colour, a
// hemisphere light and one directional light stood roughly where the sun
// would be for the level's hour, and distance fog for depth. Every one of
// those lines moves here when this file is real.
export {};
