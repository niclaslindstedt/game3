// SPDX-License-Identifier: PolyForm-Noncommercial-1.0.0
// PLACEHOLDER — the DEVELOPER OVERLAY will live here: the frame cost, the
// craft's numbers (wetted, planing, pitch, rpm, the probes), the events as
// they fire, and the REPRO line — the URL query that stands this exact
// frame up again (`?seed=&craft=&scene=&t=`), which `App.tsx`'s readers
// must be able to read back. The `debug-tools` craft. Future work: the HUD
// corner carries the build label, the seed and the craft in the meantime.
export {};
