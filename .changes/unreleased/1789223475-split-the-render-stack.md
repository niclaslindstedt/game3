---
type: Changed
title: Faster first load
---

The three.js render stack is now fetched alongside the splash instead of bundled into the page, which cuts what the game has to download before it can show you anything by about two thirds.
